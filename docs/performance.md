# Performance

Measure before optimizing. Record CPU, memory, OS, Python version, payload size, workload,
concurrency, throughput, average latency, p50, p95, and p99.

Local results are illustrative and must not be represented as enterprise production metrics.
Future work includes profiling lock contention, batching, persistent WAL, and storage-media benchmarks.
